[build]
  publish = "public"
  command = "hugo --minify"

[build.environment]
  HUGO_VERSION = "0.162.1"
  HUGO_EXTENDED = "true"

# Optional: fallback for Netlify redirects
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
